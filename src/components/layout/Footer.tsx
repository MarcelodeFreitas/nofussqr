import styles from './Footer.module.css';

export function Footer() {
  return (
    <footer className={styles.footer}>
      <span>No tracking | No redirects | No account | MIT licensed</span>
    </footer>
  );
}
